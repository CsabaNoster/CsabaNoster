---
export interface Props {
  class?: string;
}
const { class: klass } = Astro.props as Props;
---
<ul class={`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 ${klass ?? ''}`}>
  <slot />
</ul>
